# AGENTS.md

このファイルは Codex / Gemini など他の開発支援AI向けの指示書。
内容は `CLAUDE.md` と同期されている（Claude Code 用と同じプロジェクト方針）。

## 共通方針

詳細は `CLAUDE.md` を参照すること。同じ内容をここに重複させない (ドリフト防止)。

ただし、以下は **本ファイルでも必ず守る**:

### 絶対禁止事項

- 自動投票処理の実装
- 自動購入処理
- 投票サイトへのログイン処理
- 外部投票サイトへの送信処理
- 的中保証・回収率保証の表現
- 生 HTML を LLM に渡す
- サイトへの過剰アクセス（rate limit + cache 必須）
- POST 通信 (GET のみ)
- API キーをコードに直書き

### 対応言語

- ユーザー向けの説明・CLI 出力・README・エラーメッセージ・ドキュメントは **日本語** 優先
- コード中の変数・関数・クラス名は英語可

## Codex / Gemini への期待

このプロジェクトでは **Claude Code が実装** し、Codex / Gemini は以下の役割で連携する想定:

### Codex / Gemini に期待する役割

1. **レビュー**: 実装直後の差分を見て、見落とし・代替案を提示
2. **設計助言**: 大きな仕様変更前にアプローチの是非を確認
3. **検証**: テスト網羅性 / 命名 / 構造の妥当性を別視点で確認
4. **代替案**: 「ここは別実装も可能」という建設的な指摘

### やらせないこと

- Claude Code を介さずに直接コードを書く（コミットを発生させる）
- 仕様策定 (`docs/race_type_policy.md` 等) を勝手に書き換える
- このファイル (AGENTS.md / CLAUDE.md) を更新する

## レビュー依頼テンプレート

Claude Code → Codex / Gemini にレビューを依頼する際の標準テンプレート:

```markdown
## レビュー依頼

### コンテキスト
- ブランチ / コミット: <hash>
- 実装フェーズ: <例: フェーズC home_area 自動取得>

### 変更概要
- <3-5 行で要約>

### 変更ファイル
- <ファイルパス1>: <意図>
- <ファイルパス2>: <意図>

### 確認してほしい点
- <観点1>
- <観点2>

### 代替案
- <検討した代替アプローチがあれば>

### テスト観点
- <網羅すべき項目>

### 判断に迷っているポイント
- <あれば>

### 関連ドキュメント
- `docs/race_type_policy.md`
- `docs/codex_gemini_review_workflow.md`
```

## 詳細手順

`docs/codex_gemini_review_workflow.md` 参照。
