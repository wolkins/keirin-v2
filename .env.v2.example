# keirin-v2: OutputPlan v2 renderer を既定で使うための環境変数例 (2026-05-24)
#
# このファイルは `.env.example` への追加例です。
# `.env` にコピーして使ってください。

# OutputPlan v2 renderer を既定有効化
# 値: 1 / true / yes / on
# 未設定または上記以外なら v1 (既存 render_prediction)
KEIRIN_USE_OUTPUT_PLAN=1
